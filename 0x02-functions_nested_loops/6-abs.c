#include "main.h"

/**
 * _abs - print absolute value of number
 * @n : number to be checked
 * Return: absolute value
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
